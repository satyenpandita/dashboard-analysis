description "Gunicorn application server handling"

# This starts upon bootup and stops on shutdown
start on (filesystem)
stop on runlevel [016]

respawn
setuid nobody
setgid nogroup
chdir <%= current_path %>

exec <%= current_path %>/env/bin/gunicorn --workers 3 app:app